# خدمة التربية الكنسية

منصة إدارة عملية لخدمة التربية الكنسية بكنيسة رئيس الملائكة الجليل ميخائيل بدمنهور — تسجيل حضور وغياب، متابعة افتقاد، تقويم أعياد ميلاد، وإدارة أسر ومخدومين، بهوية بصرية قبطية أرثوذكسية حديثة وواجهة عربية RTL كاملة.

## المحتويات

- [التشغيل محليًا](#التشغيل-محليًا)
- [حسابات تجريبية](#حسابات-تجريبية-بعد-التزريع)
- [البنية التقنية](#البنية-التقنية)
- [البنية والصلاحيات](#البنية-والصلاحيات)
- [هيكل المشروع](#هيكل-المشروع)
- [متغيرات البيئة](#متغيرات-البيئة)
- [الهوية البصرية واللوجوهات](#الهوية-البصرية-واللوجوهات)
- [القرارات التصميمية](#القرارات-التصميمية-ولماذا)
- [ما هو مُنفَّذ فعليًا](#ما-هو-منفذ-فعليًا)

## التشغيل محليًا

**المتطلبات:** Node.js 20+ و npm.

```bash
npm install               # تثبيت الحزم (يشغّل prisma generate تلقائيًا)
cp .env.example .env      # ثم عدّل AUTH_SECRET قبل أي استخدام حقيقي
npm run db:migrate        # إنشاء قاعدة بيانات SQLite محليًا وتطبيق الجداول
npm run db:seed           # تعبئة بيانات تجريبية حقيقية (أسرة + 20 مخدوم + 3 مستخدمين)
npm run dev                # يعمل على http://localhost:3000
```

للبناء الإنتاجي: `npm run build` ثم `npm start`.

أوامر مساعدة أخرى:

```bash
npm run db:studio   # واجهة Prisma Studio لمعاينة/تعديل قاعدة البيانات
npm run lint         # ESLint
```

## حسابات تجريبية (بعد التزريع)

| الدور | اسم المستخدم | كلمة المرور |
|---|---|---|
| مسؤول عام (Super Admin) | `admin` | `Elmalak@123` |
| مسؤول مرحلة إعدادي بنين | `coordinator` | `Elmalak@123` |
| خادم أسرة الملاك ميخائيل | `servant` | `Elmalak@123` |

⚠️ **غيّر كلمات المرور هذه فورًا قبل الاستخدام الفعلي** من صفحة الإعدادات → المستخدمون (أو مباشرة من قاعدة البيانات)، فهي بيانات تجريبية معروفة للجميع في هذا المستودع.

## البنية التقنية

| الطبقة | الاختيار | السبب |
|---|---|---|
| إطار العمل | Next.js 16 (App Router) + TypeScript | Server Components + Server Actions تقلّل الكود المكرر وتبقي التحقق والصلاحيات على السيرفر |
| التصميم | Tailwind CSS v4 (Design tokens في `globals.css`) | نظام تصميم حقيقي عبر متغيرات CSS، ويدعم الوضع الداكن دون تكرار |
| قاعدة البيانات | SQLite عبر Prisma ORM | كافية تمامًا لحجم بيانات خدمة كنسية (عشرات الأسر/مئات المخدومين)، ملف واحد بلا تبعية على خادم قاعدة بيانات خارجي؛ يمكن التحويل إلى PostgreSQL لاحقًا بتغيير `provider` في `prisma/schema.prisma` وتحديث `DATABASE_URL` فقط |
| المصادقة | جلسة JWT موقّعة (مكتبة `jose`) في كوكي httpOnly + `bcryptjs` لتجزئة كلمات المرور | حل خفيف مباشر بديل عن NextAuth (الإصدار المتوافق مع React 19 كان يسبب عطل معروف في محلل تبعيات npm وقت البناء) |
| الأيقونات | lucide-react | نظام أيقونات واحد متسق |
| الحركة | CSS transitions/keyframes + تفاعلات مدروسة | حركة خفيفة بدون مكتبات ثقيلة |
| الخط | Cairo (عبر `next/font/google`) | خط عربي حديث بعائلة أوزان كاملة، ويدعم اللاتينية أيضًا |

## البنية والصلاحيات

هيكل البيانات الأساسي (`prisma/schema.prisma`) مستخرج من ملف بيانات الخدمة المرفق:

```
Stage (مرحلة، مثل "إعدادي بنين")
 └─ Family (أسرة، مثل "أسرة الملاك ميخائيل والقديس أبانوب")
     └─ Member (مخدوم)
         ├─ MemberPhone (أرقام متعددة بمسمّيات: الأب / الأم / المخدوم / البيت / الأخ)
         ├─ AttendanceRecord (حالة حضور لكل جلسة: حاضر / غائب / بعذر)
         └─ VisitationRecord (افتقاد شهري: تم/لم يتم + ملاحظة)

AttendanceSession (جلسة بتاريخ محدد داخل أسرة، وتحتوي سجلات الحضور)
User (مستخدم بدور واحد)
FamilyAssignment (ربط خادم بأسرة أو أكثر)
```

**الأدوار الثلاثة** المستخرجة من طبيعة الخدمة (كاهن اعتراف/مخدوم/خادم/أسرة/مرحلة):

- **مسؤول عام (SUPER_ADMIN):** يرى كل شيء، يدير المستخدمين والمراحل والأسر.
- **مسؤول مرحلة (STAGE_COORDINATOR):** يرى كل أسر مرحلته فقط (مثل كل أسر "إعدادي بنين").
- **خادم أسرة (FAMILY_SERVANT):** يرى فقط الأسرة/الأسر المُسندة إليه، ويسجّل الحضور والافتقاد ويدير بيانات مخدوميها.

الصلاحيات مطبّقة في طبقة الخادم (`src/lib/rbac.ts` + فحص داخل كل server action)، وليست مجرد إخفاء أزرار — أي طلب مباشر لبيانات أسرة خارج نطاق المستخدم يُرفض من نفس الدالة التي تجلب البيانات.

## هيكل المشروع

```
src/
  app/
    login/                   صفحة الدخول
    (app)/                   كل الصفحات المحمية خلف تسجيل الدخول (AppShell)
      dashboard/
      families/[familyId]/
      members/[memberId]/
      attendance/[familyId]/
      visitation/[familyId]/
      birthdays/
      settings/{users,structure}/
  actions/                   Server Actions (كل التحقق والصلاحيات هنا)
  components/
    ui/                      مكوّنات تصميم عامة (Button, Modal, Table states...)
    layout/                  الهيكل العام (Sidebar, BottomNav, Topbar...)
    domain/                  مكوّنات خاصة بالنطاق (نماذج المخدوم/الأسرة، شبكة الحضور...)
  lib/                       auth, rbac, prisma client, validation (zod), utils
prisma/
  schema.prisma
  seed.ts                    بيانات حقيقية من ملف الخدمة المرفق
```

## متغيرات البيئة

انظر `.env.example`:

- `DATABASE_URL` — مسار قاعدة بيانات SQLite (افتراضيًا ملف محلي).
- `AUTH_SECRET` — مفتاح توقيع جلسات الدخول؛ **يجب** أن يكون طويلًا وعشوائيًا وسريًا في الإنتاج (لن يعمل التطبيق بدونه).

## الهوية البصرية واللوجوهات

اللوحة اللونية (`src/app/globals.css`) استُخرجت من رؤية اللوجوهين المرسَلين مباشرة (لوجو الكنيسة الذهبي/الكريمي بأيقونة رئيس الملائكة ميخائيل، ولوجو المطرانية الأسود بالصليب القبطي): ذهبي عتيق أساسي + أخضر عميق ثانوي + خلفية كريمية دافئة، مع وضع داكن كامل مصمَّم خصيصًا (وليس مجرد عكس ألوان).

اللوجوهان الفعليان وصلا كصور مضمَّنة داخل الرسالة وليس كملفات مرفوعة يمكن الوصول لبياناتها الخام على القرص، فتعذّر استخدام ملفي الصورة الأصليين مباشرة. بدلًا من ذلك تم بناء **نسخة متجهة (SVG) مُعاد رسمها بعناية** في `public/logos/church-logo.svg` و`public/logos/eparchy-logo.svg` (مكوّنات `ChurchLogo` و`EparchyLogo` في `src/components/layout/`)، تُعيد إنتاج:

- شكل الشعار الفعلي (الإطار الذهبي المزخرف رباعي الفصوص لشعار الكنيسة، والصليب القبطي لشعار المطرانية) بدقة.
- كل النصوص العربية الفعلية كما هي («كنيسة / رئيس الملائكة / الجليل ميخائيل / بدمنهور» و«مطرانية البحيرة وتوابعها للأقباط الأرثوذكس»).
- الألوان الفعلية (ذهبي على كريمي، أسود على شفاف).

استثناءان متعمَّدان لم يُعاد رسمهما:

1. **الأيقونة المرسومة لرئيس الملائكة ميخائيل** (داخل الدائرة المركزية بشعار الكنيسة) — عمل فني تفصيلي لا يمكن إعادة رسمه بأمانة من الذاكرة، فاستُبدل برمز صليب ذهبي مبسّط بنفس الإطار.
2. **السطر القبطي الصغير أعلى شعار المطرانية** — حروف قبطية دقيقة يصعب قراءتها بثقة من صورة؛ حُذف بدل المخاطرة بنص كنسي خاطئ.

**لاستبدال هذه النسخة المتجهة بالملفين الأصليين الحقيقيين لاحقًا:**

1. ارفع ملفي الصورة بخلفية شفافة (PNG) كمرفقات فعلية (وليس لصقًا داخل الرسالة) في `public/logos/` باسمي `church-logo.png` و`eparchy-logo.png`.
2. غيّر السطر `src="/logos/church-logo.svg"` (وبالمثل لملف المطرانية) في `src/components/layout/ChurchLogo.tsx` و`EparchyLogo.tsx` إلى الامتداد `.png` الجديد.

## القرارات التصميمية ولماذا

عند تعارض متطلبين اخترت الحل الأكثر عمليةً وأمانًا وقابلية للصيانة:

- **SQLite بدل قاعدة بيانات خادم منفصلة:** حجم بيانات خدمة كنسية واحدة صغير، وSQLite يبسّط النشر (ملف واحد، بلا تبعية خارجية) دون التضحية بصحة البيانات (المعاملات، القيود الفريدة). يمكن التحويل إلى PostgreSQL بسطرين إن كبر الاستخدام لخدمة الكنيسة كلها بمراحلها.
- **مصادقة مخصّصة بدل NextAuth:** النسخة المتوافقة مع React 19 من NextAuth كانت تُفشل تثبيت الحزم (عطل معروف في محلل تبعيات npm مع هذا الإصدار)؛ حل JWT+كوكي httpOnly مباشر أبسط وأكثر قابلية للتتبع لتطبيق بحجم كهذا، مع نفس ضمانات الأمان (تواقيع موقّعة، httpOnly، secure في الإنتاج).
- **Server Actions بدل REST API منفصل:** يقلّل الكود المكرر (لا حاجة لـ fetch + API route لكل نموذج) مع إبقاء كل التحقق (`zod`) وفحص الصلاحيات على الخادم دائمًا.
- **جلسات الحضور اليومية + الافتقاد الشهري:** يعكس تمامًا الجدولين الفعليين في ملف بيانات الخدمة المرفق (شبكة شهرية للحضور بعدة أعمدة لكل شهر، وعمود واحد شهري للافتقاد).

## ما هو منفَّذ فعليًا

لا توجد شاشات "قريبًا" في هذا المشروع — كل ما يظهر في الواجهة يعمل فعليًا ومتصل بقاعدة بيانات حقيقية:

- تسجيل دخول/خروج بجلسات موقَّعة وحماية كل المسارات.
- صلاحيات ثلاثية الأدوار مطبَّقة في السيرفر (وليس فقط في الواجهة).
- إدارة كاملة (إضافة/تعديل/حذف/تعطيل) للمراحل، الأسر، المخدومين، أرقام تليفوناتهم المتعددة، والمستخدمين.
- تسجيل حضور فعلي لكل جلسة بثلاث حالات (حاضر/غائب/بعذر) مع تنقل بين التواريخ وسجل جلسات سابقة.
- متابعة افتقاد شهرية فعلية مع ملاحظات لكل مخدوم.
- تقويم أعياد ميلاد حقيقي مبني على بيانات المخدومين الفعلية، مع تمييز الشهر الحالي واليوم نفسه.
- لوحة تحكم بإحصائيات حقيقية (لا بيانات وهمية): عدد الأسر/المخدومين، نسبة الحضور والافتقاد الفعلية هذا الشهر، آخر النشاطات، أقرب أعياد الميلاد.
- بحث فوري، حالات فارغة/تحميل/خطأ مصمَّمة لكل شاشة بيانات.
- وضع داكن كامل مصمَّم خصيصًا (ليس عكس ألوان).
- Responsive حقيقي من 360px إلى الشاشات الكبيرة: جداول تتحول لبطاقات على الهاتف، تنقل سفلي على الهاتف وشريط جانبي على الشاشات الكبيرة.

تم اختبار كل هذه التدفقات فعليًا عبر متصفح Chromium حقيقي (Playwright) على أبعاد سطح المكتب والهاتف، بثلاثة أدوار مختلفة، بدون أي أخطاء في الـ console.
