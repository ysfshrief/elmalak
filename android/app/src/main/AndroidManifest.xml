<?xml version="1.0" encoding="utf-8"?>
<!--
  التطبيق نافذةٌ موثوقة (Trusted Web Activity) على الموقع المنشور.

  لا يحمل هذا الملف ولا التطبيقُ كلّه صفحةً واحدة من الموقع: يُذكر فيه عنوانُه
  فقط. فالموقع هو المصدر، وتعديلُه يصل إلى الأجهزة بمجرّد نشره.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- يُسأل عنه عند الحاجة فقط (رفع صورة المخدوم من الكاميرا). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- يسمح للتطبيق بسؤال النظام: أيّ متصفّحٍ يستطيع فتح نافذةٍ موثوقة؟ -->
    <queries>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Splash"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="@string/launch_url" />

            <!-- ألوان النظام من ألوان الموقع نفسه، فلا يبدو غريبًا عنه. -->
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/backgroundColor" />

            <!-- شاشة بدءٍ بشعار الكنيسة بدل ومضةٍ بيضاء. -->
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                android:resource="@drawable/splash_logo" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/backgroundColor" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                android:value="300" />
            <meta-data
                android:name="android.support.customtabs.trusted.FILE_PROVIDER_AUTHORITY"
                android:value="${applicationId}.fileprovider" />

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              روابط الموقع تفتح داخل التطبيق. و`autoVerify` يجعل أندرويد يقرأ
              ‎/.well-known/assetlinks.json‎ من الموقع؛ فإن وجد بصمة التوقيع
              اختفى شريطُ العنوان وصار التطبيق تطبيقًا لا متصفّحًا.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="${hostName}" />
            </intent-filter>
        </activity>

        <!-- «إدارة مساحة التخزين» في إعدادات النظام تفتح بيانات الموقع. -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.ManageDataLauncherActivity"
            android:exported="true"
            android:label="@string/manage_space">
            <meta-data
                android:name="android.support.customtabs.trusted.MANAGE_SPACE_URL"
                android:value="@string/launch_url" />
        </activity>

        <!-- تنزيلاتُ الموقع تُسلَّم إلى تطبيقات الجهاز عبر هذا المزوّد. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
